const mutations = {
  set_user(state, user) {
    state.user = user;
  },
  set_userInfo(state, userInfo) {
    state.userInfo = userInfo;
  },
  set_Roles(state, role) {
    state.role = role;
  },
};
export default mutations;
