const getters = {
  routes(state) {
    return state.routes;
  },
  addRoutes(state) {
    return state.addRoutes;
  },
  permissions(state){
    return state.permissions;
  }
};
export default getters;
